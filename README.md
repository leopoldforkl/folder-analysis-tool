# folder-analysis-tool
This is a simple python script that writes a github repo to a text-file. Can be useful for LLM chats.

Just copy the python script into the root of a github repo and run with:
```bash
python map_repo.py
```